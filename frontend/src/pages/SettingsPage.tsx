import { PageScaffold } from "./PageScaffold";

export function SettingsPage() {
  return (
    <PageScaffold
      title="Settings"
      description="Profile, preferences, integrations, and notification settings will be added after auth is formalized."
    />
  );
}
