import { PageScaffold } from "./PageScaffold";

export function WatchlistPage() {
  return (
    <PageScaffold
      title="Watchlist"
      description="Track symbols outside current holdings and route market data requests through backend APIs only."
    />
  );
}
