import { PageScaffold } from "./PageScaffold";

export function AdminPage() {
  return (
    <PageScaffold
      title="Admin"
      status="coming-soon"
      description="Internal diagnostics and support views are intentionally deferred beyond the MVP shell."
    />
  );
}
