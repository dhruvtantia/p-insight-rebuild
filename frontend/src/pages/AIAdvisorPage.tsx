import { PageScaffold } from "./PageScaffold";

export function AIAdvisorPage() {
  return (
    <PageScaffold
      title="AI advisor"
      description="Portfolio Q&A will call only backend AI endpoints that build structured context before returning explanations."
    />
  );
}
